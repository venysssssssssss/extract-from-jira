version: "3.9"

services:
  jira-extractor-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jira-extractor-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./output:/app/output
    restart: unless-stopped
